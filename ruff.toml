line-length = 100
target-version = "py310"
src = ["src", "tests", "."]
exclude = [
  ".git",
  ".github",
  ".venv",
  "venv",
  "build",
  "dist",
  "__pycache__",
]

[lint]
select = ["E", "F", "I", "B", "UP"]
ignore = [
  "E501",  # Line too long (handled by formatter)
]
